# 擬ポテンシャル読み込みルーチン改造計画

## 経緯

前回の静的調査結果は [report_pp.tex](report_pp.tex) に記録した。対象は主に `s_pp_info`、`input_pp`、`prep_pp` であり、形式別リーダ、初期密度、SO、PAW の関連経路も追跡した。調査はソースコードの読み取りに基づき、形式別の数値実行による検証はまだ行っていない。

調査で、容量を表す `lmax0` / `lmax` と `nrmax0` / `nrmax` は名前が似ている一方で用途が違うと分かった。`0` という接尾辞から用途の違いを推測しにくいため、今後の改造に先立つ可読性改善として改名を TODO に加える。

| 現在の `s_pp_info` メンバ | 改名案 | 意味 |
| --- | --- | --- |
| `lmax0` | `lmax_work` | 読み込み・前処理の作業配列の角運動量容量と入力検査の上限 |
| `nrmax0` | `nrmax_work` | `upp`、`vpp` などの作業配列の動径添字上限。配列は `0:nrmax_work` |
| `lmax` | `lmax_table` | 元素別テーブルと格子展開用配列の角運動量容量。改名は候補 |
| `nrmax` | `nrmax_table` | 元素別動径テーブルの確保範囲。改名は候補 |

`mr(ik)` は入力由来の動径範囲、`nrps(ik)` は非局所部分の有効範囲であり、上記の容量とは別に扱う。実空間格子点数は `s_pp_grid` 側の `mps` / `nps` が表す。

## TODO

- [ ] `s_pp_info` の `lmax0` を `lmax_work`、`nrmax0` を `nrmax_work` に改名し、初期化、参照、コメントを揃える。`lmax` / `nrmax` の `*_table` への改名は、その際に参照箇所と影響を確認して判断する。
- [ ] 改名前に `s_pp_info` メンバの全参照を洗い出す。現時点で関連する Fortran ファイルは `src/common/structures.f90`、`src/atom/pp/{salmon_pp,input_pp,prep_pp,read_ps_upf_module,read_paw_upf_module}.f90`、`src/so/prep_pp_so.f90`、`src/plusu/prep_pp_plusu.f90`、`src/io/write.f90`。文書や入力例の参照も確認する。
- [ ] 改名では配列境界と添字規約を保つ。特に作業配列の `0:nrmax_work` と、元素別テーブルの `1:nrmax` に対応する処理、入力点数 `mr`、カットオフ `nrps` を混同しない。
- [ ] 擬ポテンシャル読み込みの機能変更を具体化した時点で、対象形式、入力データ、期待する保存先・後段の利用先を明記する。ファイルに情報が存在することと、リーダが保存・利用することを分けて設計する。
- [ ] 機能変更に関係する経路では、`report_pp.tex` の注意点を再確認する。特に元素間の作業配列・形式フラグの持ち越し、PSP8 密度の読み捨て、通常 UPF と PAW-UPF の保存経路、NLCC 微分、SO と masking のカットオフ更新を確認対象とする。
- [ ] 実装後は対象形式を使う入力で読み込み結果と後段の動作を確認する。改名だけの段階でも、未置換の旧メンバ名と配列境界の差分を確認する。

## 価電子密度を `rho_pp_tbl` に接続する計画

### 現状と形式別の方針

`rho_pp_tbl(i,ik)` は三次元密度そのものではなく、`calc_density_pp` が `rho_pp_tbl(i,ik) * (rad(i+1,ik)-rad(i,ik))` と積分する動径分布である。通常の三次元球対称密度を `n(r)` とすれば、目標は `4πr²n(r)` に相当する値をここへ入れること。`calc_density_pp` の積分範囲は `1:mr(ik)` であり、`nrps` ではない。`G=0` 成分は `zps` から別に設定する。

| 形式 | 現状 | 改変方針 |
| --- | --- | --- |
| `ABINITPSP8` | `read_ps_abinitpsp8` は末尾の価電子密度らしきブロックを `rho_tmp` に読み、`sum(rho_pp)` を表示して捨てる。射影子形式なので波動関数からの密度生成にも入らず、`method_init_density='pp'` は形式名による検査で停止する。 | ブロックの存在と単位・規約を確認して `rho_pp_tbl(:,ik)` に変換・保存する。末尾ブロックがない標準 PSP8 も考慮し、存在しない場合の処理を明示する。 |
| `ABINITFHI` (`.fhi`) / `FHI` (`.cpi`) | 擬波動関数を `upp` に読む。masking なし、かつ `method_init_density/='wf'` なら `making_ps_without_masking` が `(2l+1)*upp²` を蓄積する。masking ありでは生成しない。末尾の NLCC はコア密度で、価電子密度とは別。 | 密度生成を masking の有無から独立させ、`upp` が変換される前に一度だけ行う。占有数の情報を別途得られる場合はそれを使用する。得られない場合、既存の縮退度による蓄積は近似として扱い、既知の参照占有数がないことを記録する。 |
| `KY` | `rhopp` を読むが保存しない。一方、波動関数由来の近似密度は条件付きで生成される。 | ファイル由来の `rhopp` の物理量・単位を確認し、使用可能なら直接密度を優先する。 |
| 通常 `UPF` | `PP_RHOATOM` を `rho_pp_tbl` に保存する。 | 既存動作を基準にし、タグが欠ける場合を「密度なし」として扱う。 |
| `PAW-UPF` | 密度は `paw%rhops` に保存され、通常の `rho_pp_tbl` 経路には入らない。 | PAW 固有の規約と後段処理を確認して別途設計する。通常 UPF と一括で扱わない。 |

PSP8 の末尾ブロックについては形式差を先に確かめる。SALMON2 の現リーダはコア密度ブロックの後に `mmax` 行を無条件に読むが、[ABINIT の PSP8 形式説明](https://docs.abinit.org/developers/psp8_info/)はコア密度を最後のブロックとして記載する。したがって「すべての PSP8 に価電子密度ブロックがある」という前提では実装しない。FHI 系についても、[ABINIT の format 6 説明](https://docs.abinit.org/developers/psp6_info/)で示される末尾の密度はコア補正用である。

### 実装順序

1. `s_pp_info` に元素別の `logical` 配列 `has_rho_pseudo(:)` と `has_wf_pseudo(:)` を追加し、初期値を偽にする。前者は `rho_pp_tbl` に値を保存したこと、後者は `upp` にファイル由来の擬波動関数を読んだことを表す。root 側でデータを得た時に設定し、MPI broadcast する。PAW の `paw` 配列にあるデータはこの二つのフラグの対象外とする。将来の密度可否判定には前者を用い、電荷積分などの妥当性検査も組み合わせる。
2. PSP8 リーダの末尾ブロックをファイルごとに検出し、存在する場合は `r_tmp` と `rad(i,ik)` の一致、有限性、非負性、点数・容量を確認する。元データが `n(r)`、`4πn(r)`、`4πr²n(r)` のどれかを仕様・実ファイルで確定してから `rho_pp_tbl(i,ik)` に変換する。現在の `sum(rho_tmp*r_tmp**2)*dr` という診断式だけを規約の証拠にしない。原点と外側の値も明示的に設定する。
3. FHI 系と他の波動関数入力形式の密度生成を `making_ps_without_masking` から独立した共通処理へ移す。各元素のリーダ終了後、masking や `upp` の半径・角度因子変換の前に実行する。直接密度を保存できた場合は生成で上書きしない。`method_init_density='pp'/'pp_magdir'` のときに必要な処理とし、`read_dns_cube` 用の不要な生成を避ける。
4. 波動関数からの生成では、占有数が既知ならその値を使用する。占有数が不明で、初期 SCF 用の粗い密度を得る場合は「各角運動量チャネルに同数の電子を割り当てる」近似を使う。使用可能なチャネル数を `N_ch`、変換前の波動関数を `u_l(i)`、`Δr_i=rad(i+1,ik)-rad(i,ik)`、`I_l=Σ_{i=1}^{mr(ik)} u_l(i)^2 Δr_i` とすると、`rho_pp_tbl(i,ik) = [zps(ik)/N_ch] Σ_l u_l(i)^2/I_l` とする。これにより `calc_density_pp` と同じ離散積分で総電荷が `zps(ik)` になる。ここでの「均一」は角運動量チャネルごとの総電子数が同じという意味であり、磁気量子数ごとの占有が同じという意味ではない。`I_l` がゼロ・非有限のチャネルは除外して `N_ch` を数え直し、全チャネルが無効なら密度生成失敗とする。現行の `(2l+1)` 蓄積と途中打ち切りはこの式で置き換える。入力ファイルに直接の価電子密度がある場合は直接値を優先する。この近似の用途は SCF 初期密度に限る。
5. `input_pp` の `method_init_density='pp'/'pp_magdir'` の可否判定を、形式名と射影子フラグによる判定から `has_rho_pp(ik)` に変更する。全元素について密度が利用可能か確認し、欠ける元素とファイル名を示す。`wf` と `read_dns_cube` の処理にはこの密度を要求しない。
6. `calc_density_pp` と同じ離散積分 `Σ_{i=1}^{mr} rho_pp_tbl(i,ik) Δr_i` を診断に用い、`zps(ik)` とのずれを記録する。単位・入力異常による大きなずれは失敗として扱う。有限範囲の裾による小さなずれを補正するかは、許容値と補正方法を決めてから実装する。`G=0` だけ `zps` を代入する現行処理と `G→0` の連続性も確認する。
7. 元素ごとに `flag_beta_proj_is_given` などの形式状態を初期化する。前元素の状態が FHI の密度生成と利用可否判定に影響しないようにする。

### 実装時の確認項目

- PSP8 は密度ブロックあり・なし、NLCC あり・なしを分け、ファイル位置と `mr=mmax-1` の添字対応を確認する。
- FHI 系は masking あり・なしで同じ変換前波動関数から密度を作り、既存の masking なし経路との差を評価する。
- FHI 系の波動関数について、各チャネルの `I_l`、有効な `N_ch`、生成した密度の積分値を記録する。`Lmax_ps` で計算用チャネルを制限する場合、初期密度に使うチャネル集合をファイル内の波動関数に基づいて明示的に決める。
- 直接密度のある通常 UPF と KY、密度のない射影子形式、混合元素、serial と複数 MPI rank で `method_init_density='pp'` の可否と電荷積分を確認する。
- `rho_pp_tbl` は初期電子密度用であり、NLCC の `rho_nlcc_tbl` や PAW 側の密度と混同しない。

上記の密度読み込み・生成方式と、フラグを利用した可否判定は、現時点では計画段階である。

### 2026-09-25 実装メモ

最初の工程として二つのフラグの宣言・確保・偽への初期化・MPI broadcast を追加した。`comm_bcast` に logical 配列用の手続きがないため、通信は元素ごとに scalar logical として行う。`has_wf_pseudo` は KY、ABINIT、ABINITFHI、FHI の読み込み成功後に真にする。`has_rho_pseudo` は通常 UPF の `PP_RHOATOM` 読み込み時、または既存の masking なしの波動関数由来密度生成で正の積分値が得られた時に真にする。PSP8 密度の保存、FHI 系の masking ありでの密度生成、フラグによる `method_init_density` 判定は未実装である。
